name: shadownet
description: Operación Camaleón - ShadowNet Agent Profile

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Tus compañeros pueden agregar google_fonts aquí (punto 4)
  # google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/hacker.png
    - assets/images/enforcer.png
    - assets/images/ghost.png